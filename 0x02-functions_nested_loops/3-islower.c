#include "main.h"

/**
 * _islower - checks if a charater is lowwer cases
 * @c: the charater
 * Return: 1 if lower case or 0 ir not
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1)
	else
		return (0)
}
